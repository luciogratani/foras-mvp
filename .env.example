# Supabase project URL (find it in your Supabase dashboard → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=

# Supabase anonymous key — safe to expose in the browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# PostgreSQL schema for this tenant (e.g. "template", "bar_rossi")
NEXT_PUBLIC_SUPABASE_SCHEMA=template
