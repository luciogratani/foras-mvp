/** @type {import('next').NextConfig} */
const nextConfig = {
  poweredByHeader: false,
  // images.remotePatterns per il CDN media (offland.wedesignwecode.com)
  // sarà valutato in fasi successive, quando arriveranno contenuti reali.
}

export default nextConfig
