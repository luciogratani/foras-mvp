# @repo/hau — hau.studio (rebuild)

App Next.js standalone per la ricostruzione del sito vetrina hau.studio.
Non è un tenant foras: nessuna dipendenza da Supabase o `@repo/*`. Gira su porta 3002.

## Dev

```bash
pnpm --filter hau dev   # → http://localhost:3002
```
